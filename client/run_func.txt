
function(run_client)
	message("Working script")

	#foreach(i RANGE 106 152 1)
	foreach(i RANGE 20 24 1)
		execute_process(COMMAND ${CMAKE_CURRENT_BINARY_DIR}/client_app -p 12345 -s localhost -i "${CMAKE_CURRENT_BINARY_DIR}/resources/Xixi_F/query_files/${i}.txt")
	endforeach()
endfunction()

run_client()

